version: 2.1
jobs:
  build:
    machine:
      docker_layer_caching: true
    steps:
      - checkout
      - run: |
          set -ex
          pwd
          ls -la
          docker version
          make build

workflows:
  version: 2
  build-and-test:
    jobs:
      - build
